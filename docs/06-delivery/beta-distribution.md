# Beta Distribution

Phase 8 (`docs/01-product/roadmap/phases.md`): TestFlight (iOS) + Play Internal Testing (Android), targeting ~20–50 users.

- Distribute via EAS Build's `preview` profile (see `apps/mobile/eas.json`), uploaded to TestFlight/Play Console.
- Collect feedback via a shared form/channel (TBD — add the link here once established) rather than scattered DMs, so nothing gets lost.
- Track crash-free rate (Sentry) and cost-per-active-user (OpenAI spend) during beta specifically — both are exit criteria for Phase 8, not just launch-week concerns.
