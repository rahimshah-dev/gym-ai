# Support Handoff

Not yet needed pre-launch — populate before Phase 9 (Launch) with:
- Where support requests come in (email inbox, in-app form, etc. — TBD).
- Common issue → resolution mapping once real usage data exists (e.g. "generation failed" → check `docs/03-architecture/app-architecture.md` §4.5 cost/rate-limit dashboards first).
- Escalation path from support to engineering for bugs vs. account/billing issues (billing issues route to RevenueCat's dashboard/support tools first).
