# AI Templates

Reusable prompt templates for recurring AI-assisted tasks in this repo (e.g. "port a component from apps/web-demo," "add a new domain entity"). Empty scaffold — populate as recurring patterns emerge. General-purpose templates (PRD, ADR, feature spec) live in `docs/07-templates/` instead; this folder is specifically for AI-prompt templates, not document templates.
