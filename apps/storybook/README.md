# @gymai/storybook

Optional component playground for `@gymai/ui`. Deferred — not required until `packages/ui` has enough components that iterating via `apps/mobile` gets slow. See `AGENTS.md` in this directory for setup notes when that time comes.
