# Examples

Optional reference implementations — a worked example of a full feature slice (route → feature component → domain type → API endpoint) once one exists, useful as an onboarding reference for new contributors. Empty until the first real feature (Phase 3) is built end-to-end and worth extracting as a teaching example.
