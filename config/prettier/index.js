/** Shared Prettier config — referenced from each app/package's own config or package.json "prettier" field. */
module.exports = {
  semi: true,
  singleQuote: false,
  trailingComma: "none",
  printWidth: 100,
  tabWidth: 2
};
