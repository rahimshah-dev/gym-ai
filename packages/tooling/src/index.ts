// Codegen/scripts/dev-tooling helpers shared across the monorepo's scripts/
// directory (e.g. shared logic for scripts/sync-tokens.ts and
// scripts/generate-icons.ts, if those grow beyond simple one-off scripts).
export {};
