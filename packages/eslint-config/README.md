# @gymai/eslint-config

Shared ESLint config, extended by every app/package's own `.eslintrc`. See `index.js` — currently a thin wrapper around `eslint-config-universe/native`, with a TODO to encode the architecture boundary rules from root `AGENTS.md` as lint rules once there's enough real code to make that worthwhile.
