// Shared client/UI state patterns and selectors — see
// docs/03-architecture/adr/0004-state-strategy.md for what belongs here
// vs. @gymai/api's server-cache layer vs. @gymai/auth's session state.
// Empty until Phase 2 needs a concrete shared store (e.g. the guided-session
// timer state machine, if it turns out to be reused across screens).
export {};
