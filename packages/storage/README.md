# @gymai/storage

Secure storage (auth tokens, via `expo-secure-store`) and general cache (offline-viewable plans) interfaces. Keep these two paths separate — tokens never go through the general cache.
