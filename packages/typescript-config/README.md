# @gymai/typescript-config

Shared `tsconfig` bases: `base.json` (plain TS packages, extends root `tsconfig.base.json`) and `react-native.json` (for `apps/mobile` and `packages/ui`, adds RN-appropriate `jsx`/module resolution).
