# @gymai/validation

Shared Zod validation schemas. `setupFormSchema` ports the exact validation rules from `apps/web-demo/index.html`'s setup form (time range 5–180 minutes, equipment required per mode) so `apps/mobile` doesn't silently drift from the proven mockup rules.
